spring:
  datasource:
    username: root
    password: cjc178044
    url: jdbc:mysql://127.0.0.1:3306/dbcourse?useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:/mapper/*.xml

server:
  port: 8080